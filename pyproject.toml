[project]
name = "spark-batch-foundations"
version = "0.1.0"
description = "Portfolio project to learn PySpark foundations with Docker and uv."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "pyspark>=4.0.0",
]

[tool.uv]
package = false
